package org.example.model;

/**
 * classe enumeration che rappresento lo stato di un to-do, serve per tenere traccia se un'attività è completata o meno
 */
public enum StatoToDo {
    COMPLETATO,
    NONCOMPLETATO
}
